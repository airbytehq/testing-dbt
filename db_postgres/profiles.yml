config:
  partial_parse: true
  printer_width: 120
  send_anonymous_usage_stats: false
  use_colors: true
tests:
  outputs:
    prod:
      dbname: postgres
      host: localhost
      pass: password
      port: 4000
      schema: tests
      threads: 32
      type: postgres
      user: postgres
  target: prod
